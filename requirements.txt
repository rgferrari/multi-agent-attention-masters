# python_version = 3.10
# Notes:
#  - numpy is capped at 2.2.x because newer numpy (>=2.3) requires Python >=3.11.
#  - baselines pulls gym[atari,classic_control,mujoco,robotics], which pins
#    pygame==2.1.0; that has prebuilt wheels only through Python 3.10, so 3.10
#    is required for a no-compile install.
#  - torch's CUDA 12.1 build is hosted on the PyTorch index below.
--extra-index-url https://download.pytorch.org/whl/cu121

baselines==0.1.5
gym==0.26.2
gymnasium==1.3.0
gymnasium-robotics==1.4.2   # MAMuJoCo (Multi-Agent MuJoCo) envs; works with mujoco 2.2.0
huggingface_hub==1.17.0
mpe2==1.1.0
numpy==2.2.6
PyYAML==6.0.3
seaborn==0.13.2
setuptools<71
tensorboard
torch==2.5.1+cu121
tqdm==4.67.3
